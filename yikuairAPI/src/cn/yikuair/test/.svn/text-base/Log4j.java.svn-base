package cn.yikuair.test;



import org.apache.log4j.Logger;
import org.apache.log4j.PropertyConfigurator;

public class Log4j {

	/** 
	 * @Title: main 
	 * @Description: TODO(这里用一句话描述这个方法的作用) 
	 * @param @param args    设定文件 
	 * @return void    返回类型 
	 * @throws 
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		PropertyConfigurator.configure(Log4j.class.getResource("/conf/log4j.properties"));
		Logger logger = Logger.getLogger(Log4j.class);
		logger.debug("debugLog4j配置详解及不同的包(package)下的Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详" +
				"解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同" +
				"的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同" +
				"的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(pa" +
				"ckage)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(packa" +
				"ge)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志" +
				"写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日" +
				"志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4" +
				"j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的" +
				"包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(pa" +
				"ckage)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日" +
				"志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日" +
				"志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详" +
				"解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下" +
				"的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下" +
				"日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包" +
				"(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(pa" +
				"ckage)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写" +
				"入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日" +
				"志文件下Log4j配置详解及不同的包(package)下的日志写入到不同的日志文件下091");
		logger.error("error");
		logger.info("info");
		logger.warn("warn");
		System.out.println("system out println");
	}

}
